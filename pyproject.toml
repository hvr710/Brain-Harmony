[project]
name = "brainharmonix"
version = "0.1.0"
dependencies = [
]

[tool.setuptools.packages.find]
include = ["modules*", "configs*", "libs*", "datasets*", "checkpoints*", "experiments*", "brainharmony_downstream*"]
exclude = [
    "tests*", 
    "*.pyc", 
    "__pycache__",
]
